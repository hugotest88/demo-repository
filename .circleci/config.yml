version: 2.1
jobs:
  build:
    docker:
    - image: cimg/openjdk:21.0.2
    - image: circleci/openjdkjdk:21.0.2
    steps:
    - checkout
    - run:
        command: |
          mvn -B package --file pom.xml
workflows:
  Java CI with Maven:
    jobs:
    - build:
        filters:
          branches:
            only:
            - main
            - testTranslator
